ADMIN_ID=admin
ADMIN_PASSWORD=changeme123

# Comma-separated list of allowed frontend origins for CORS.
# Defaults to the production Vercel URL + local dev if unset.
CORS_ORIGINS=https://freefirefrontend.vercel.app,http://localhost:5173,http://127.0.0.1:5173
